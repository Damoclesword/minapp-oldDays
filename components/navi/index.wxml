<view class="container">
    <image bind:tap="onLeft" src="{{latest ? disLeftSrc : leftSrc}}" />
    <text class="title">{{title}}</text>
    <image bind:tap="onRight" src="{{first ? disRightSrc : rightSrc}}" />
</view>