<view hidden="{{hidden}}" class="container">
    <image class="header-image" src="{{imgSrc}}" />
    <image class="tag" src="images/moive@tag.png" />
    <text class="content">{{content}}</text>
</view>